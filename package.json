{
  "name": "utunnel",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:edge": "bun --cwd apps/edge run dev",
    "dev:agent": "bun --cwd apps/agent run dev",
    "demo:v1": "bun run smoke:v1",
    "smoke:v1": "bun run packages/testkit/src/v1-smoke.ts",
    "typecheck": "bun x tsc --noEmit -p packages/protocol/tsconfig.json && bun x tsc --noEmit -p packages/config/tsconfig.json && bun x tsc --noEmit -p packages/testkit/tsconfig.json && bun x tsc --noEmit -p apps/edge/tsconfig.json && bun x tsc --noEmit -p apps/agent/tsconfig.json && bun x tsc --noEmit -p apps/web/tsconfig.json",
    "test": "bun test ./packages ./apps",
    "check": "bun run typecheck && bun run test"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.6.3"
  }
}
